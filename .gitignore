.venv/
.idea/
__pycache__/
*.pyc
*.egg-info/
.DS_Store

# Claude Code local machine settings (permissions allowlist etc.) — not shared config
.claude/settings.local.json

# Model checkpoints and HF cache
checkpoints/
*.safetensors
*.bin

# Experiment outputs are small JSONL — keep legacy fixtures; ignore bulky artefacts.
experiments/**/*.pt
data/silver/
runs/

# Locally-built native deps (OpenFst/pynini) — platform-specific binaries,
# rebuilt from source per-machine. See STEP_1A_RESULTS.md for build steps.
vendor/

# Local stand-in for the Drive results/cache root
local_run/
